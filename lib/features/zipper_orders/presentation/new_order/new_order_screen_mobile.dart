import 'package:flutter/material.dart';
import 'package:hooks_riverpod/hooks_riverpod.dart';

class NewOrderScreenMobile extends ConsumerWidget {
  const NewOrderScreenMobile({
    super.key,
  });

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return const Center(
      child: Text('NewOrderScreenMobile'),
    );
  }
}
